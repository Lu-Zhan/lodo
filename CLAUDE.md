# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 项目定位

lodo 是一个"纠缠式提醒"待办 app:到期提醒后可 完成/稍等,忽略或稍等都会每隔一个稍等间隔(默认 15 分钟)重复提醒直到完成。三个平台实现,**语义必须逐字对齐**:

- `web/` — Streamlit 演示版(参考实现,仅作演示)
- `ios/` — SwiftUI 多平台 app(iOS 17+/macOS 14+,SwiftData;最终交付目标)
- `android/` — Kotlin + Jetpack Compose + Material 3(minSdk 31——为端上 AI/Gemini Nano 的 AICore SDK 硬性要求从 26 提高,详见下文"端上 AI"）

所有 UI 文案为中文,三端逐字一致。iOS UI 只用 SwiftUI 系统控件,不自绘、不引第三方库——唯一经用户明确确认的例外是 `ContactGraphView.swift`(人脉关系图谱,`Canvas` 绘制节点连线),别拿它当先例引入其他自绘 UI。健康页的趋势图用 **Swift Charts**(`import Charts`)、旅行页的地图用 **MapKit 的 SwiftUI `Map`**——两者都是 Apple 的系统框架、声明式用法,和"只用系统控件、不引第三方库"不冲突,**不是**新的自绘例外,别拿它们当引入绘图/地图库的先例。Web 相关文件(含 SQLite 数据、.env)全部放 `web/` 内,不放仓库顶层。

## 常用命令

```bash
# Web
cd web && pip install -r requirements.txt
streamlit run app.py
python -m pytest tests/                                  # 调度器测试

# iOS — 核心逻辑包可独立测试(无需模拟器)
cd ios/LodoCore && swift test
swift test --filter SchedulerTests                       # 单个测试类
# App 本体用 Xcode 打开 ios/Lodo.xcodeproj。注意:UI 已接入 iOS 26
# Liquid Glass API(#available 门控),编译需要 Xcode 26 及以上(iOS 26 SDK 起)。

# Android(本机 JDK 17 经 Homebrew 安装,gradle 命令前必须设 JAVA_HOME)
export JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home
cd android
./gradlew :app:testDebugUnitTest                         # 调度器单测
./gradlew :app:testDebugUnitTest --tests "com.lodo.app.core.SchedulerTest.dueAtTime"
./gradlew :app:assembleDebug                             # APK 在 app/build/outputs/apk/debug/
```

## 跨平台对齐(改一处必须同步三处)

三端各有一份纯逻辑调度器 + 1:1 移植的同名测试用例(基准时间均为 2026-07-08 周三 09:00):

| | 调度器 | 测试 |
|---|---|---|
| web(参考实现) | `web/lodo/scheduler.py` | `web/tests/test_scheduler.py` |
| iOS | `ios/LodoCore/Sources/LodoCore/Scheduler.swift` | `ios/LodoCore/Tests/LodoCoreTests/SchedulerTests.swift` |
| Android | `android/.../core/Scheduler.kt` | `android/.../core/SchedulerTest.kt` |

关键共享语义(改动调度行为时三端及测试同步改):

- **枚举持久化字符串**三端一致:status `pending/done`、phase `start/end`、repeat_type `none/daily/weekly`
- **周几 0=周一 … 6=周日**(Swift 里 `(weekday+5)%7`,Kotlin 里 `DayOfWeek.value - 1`)
- **重复时间点**是 `"HH:MM"` 字符串列表,一天可多个;`nextOccurrence` 做 8 天前瞻
- **时长两阶段**:duration>0 的事项先提醒"该开始了"(phase start→end),结束再提醒"完成了吗"
- **markNotified 语义**:提醒发出即把 nextRemindAt 顺延一个稍等间隔(忽略也会重响)
- **重复事项完成一次**:保持 pending 并顺延到下一次发生,同时插入一条 done 历史记录
- **设置默认值**:稍等 15 分钟、全天提醒 09:00、每日汇总 21:00

**DeepSeek prompt 三端逐字一致**:`web/lodo/ai.py`、`ios/LodoCore/Sources/LodoCore/DeepSeekClient.swift`、`android/.../ai/DeepSeekClient.kt`。接口:`parse`(新建)、`edit`(改单个事项)、`command`(AI 总入口,目前仅 iOS/Android 有——携带全部待办列表;客户端必须校验 uuid 在列表内,且返回后用**最新**列表重新匹配)。iOS/Android 把 prompt 拆为 taskSchema + taskRules 拼装,web 仍是整段 `_FORMAT_AND_RULES`,文字内容一致。错误文案("未配置 DeepSeek API key…"/"调用 DeepSeek 失败:…"/"无法解析:…")三端一致。改 prompt 时同步三端。iOS 把 `command` 的总则/待办规则/记忆规则进一步拆成 `agent.md` + 两个可在设置里编辑/重置的 skill(`AgentSkillStore`,内置默认文本 + Application Support 下的覆盖文件),默认内容仍与 web/Android 逐字一致;用户主动自定义后本机 prompt 会偏离默认值,属预期行为,不算破坏三端对齐。

**iOS/Android 已对齐的 AI 协议**(web 演示版仍是旧的 parse/edit):`command` 为 `{"actions": [create/update/complete/delete, ...]}` 数组协议(批量、完成、删除),批量/完成/删除过确认页;**单条新建两端已分叉**——Android 仍是可编辑表单、保存后才落库,iOS 改成和单条修改一样**默认直接落库**(用户在对话里说的就是要加这件事,多点一次确认没带来信息量),结果卡片本身就是个开关:最左边一枚蓝色对号 = 这条有效,点一下删掉事项、对号变灰 ✕、抬头文案换成「已取消新建」,再点一下按同一份快照重建(uuid 换新的,连同 `createdRemoved` 一起写回快照——改写 `taskSnapshotData` 也是让这条气泡重新渲染的触发点,`@Query` 盯的是消息,光删 `TaskItem` 不会刷新)。这个开关**不限「最新一条」**:它动的就是卡片上写着的那一条,不依赖当前上下文,老对话里已经存下的 `taskProposal` 提案卡仍照常渲染、按钮照常可用(`AgentTaskSnapshot.createdUUID` 区分:新流程的新建结果带它、给 ✕,老流程确认过的结果没有、不给按钮);单条修改**不弹确认**,AI 一解析完直接落库(iOS/Android 都复用批量执行同款的 `UndoOp.updated`/`Updated` 撤销快照机制兜底解析错误——iOS 结果卡片上带撤销按钮[箭头图标,语义是「改回原样」,和新建那颗对号开关有意区分;它依赖 `lastUndo` 上下文,所以仍然只在最新一条可点],Android 是 Snackbar 撤销)。iOS 的 `memorize`/`auto_memorize` 结果卡片同理带 ✕(删条目走 `MemoryPipeline.delete`,连向量分片一起清,并把那条消息原地改写成「已取消收藏。」——条目没了卡片本来也渲染不出来)。`suggest_memorize` 不在此列:那是 AI 主动建议、用户没要求,仍然点了「收藏这条」才落库。关键信息缺失时的反问**两端已经分叉**——Android/web 仍是单问题的 `{"question", "options"}`(候选渲染成输入栏上方的胶囊行),iOS 换成了多问题的 `{"ask": [{"header", "question", "multi_select", "options": [{"label", "description", "recommended"}]}]}`(参考 Claude app 的提问卡片:可翻页、单选/多选、带推荐项和"其他"自由输入,答完原地变成只读记录卡,选择静默随下一轮请求回传给 AI,不冒用户气泡;**卡片待答期间 iOS 把整条输入区收起来**——问题摆着等选、底下再留个输入框是两个并行入口,想自由回答用卡片自带的「其他」,不想答点卡片上的取消)。iOS 侧 `AICommandResult.clarify` 已删,连带 `AgentMessageKind.clarify`/`AgentMessage.clarifyOptions` 换成 `ask`/`askResult` + `askSnapshotData`(`AgentAskSnapshot`,老库里的 clarify 消息自动降级成纯文本气泡);UI 在 `ios/Lodo/Views/AgentAskCard.swift`,解析在 `DeepSeekClient.parseAsk`(离线单测)。Watch 屏幕小,只展示第一道题、选完带补充重新解析。**不要按"三端对齐"把 iOS 的 ask 回退成 clarify**,要动就是把 Android 也升级成 ask。`suggestDuration`/`updateMemory`(时长记忆)、`summarizeToday`(汇总正文)、`weeklyInsight`(完成洞察)、`suggestReschedule`(改期候选)双端同语义同 prompt。`suggestTodayHandling`(总览 tab 今天待办处理建议)、`summarizeTodayMemories`(总览 tab 今天新增记忆总结)是仅 iOS 的两个新函数,与 `weeklyInsight` 同构(同一套"薄包装 + 返回一句话 JSON"写法),Android 没有总览 tab,不需要跟进。`runRoutine`(定时任务,见下)同样仅 iOS、同一套写法,区别是指令来自用户而不是写死的 prompt,并且允许 ReAct 联网(`parseRoutine` 是可离线单测的纯解析函数)。`command` 的 `memorize`/`ask_memory`(收藏与记忆问答,`memoryEnabled` 开关)、`suggest_memorize`(AI 主动建议收藏,不落库、气泡上"收藏这条"按钮点了才存,和 `memorize` 走同一个 `memoryEnabled` 开关但归一化规则和 `ask_memory`/`answer` 一组——与写操作混在一句话里时会被丢弃,只在这句话*唯一*意图就是陈述一条值得记的信息时触发)目前**仅 iOS**(侧栏「AI 助手」页统一入口),Android 尚未跟进,不要按"三端对齐"回退这段。`auto_memorize`(对话中顺带提到的重点事实/事件,如"班主任喜欢收贺卡",不是用户明确要求收藏,也不用像 `suggest_memorize` 那样等用户点按钮确认,可以和其他操作同时出现在一句话里,例如一边新建待办一边顺带记下一件事)同样**仅 iOS**,和 `suggest_memorize` 共用 `memoryEnabled` 开关但不参与其归一化分组(不会因为混了写操作被丢弃,这点和 `memorize` 同组)。`title`/`text` 由 `command` 这轮请求直接给出,`MemoryPipeline.saveAutoMemory` 跳过 `memorize()` 那次额外的整理调用直接落成 `ready` 状态(省一次网络请求),打保留标签 `MemoryItem.autoTagName`("AI记录")与用户主动收藏/确认过的记忆区分开,正常显示不参与资产/人脉那套隐藏筛选,也被排除在 `MemoryTagManageView` 的改名/删除列表之外(和 `assetTagName` 同样的保护);这个标签也被排除在 `memorize()` 的 `existingTags` 提示之外,避免常规收藏整理时被复用混淆。`saveAutoMemory` 客户端去重(同 `AgentPreferences.append` 的"互相包含即算重复"判定):新内容是已有某条自动记录的更完整表述(包含旧摘要且更长,如过敏原后来又多了一种)时原地更新那条,不新开一条;新内容被已有摘要包含(纯重复提及)时跳过。route() 里和 `remember_preference` 同一套"先摘出来静默落盘,再走剩下的动作"处理,不进确认页、不参与撤销。时长记忆文件:iOS 在 Application Support,Android 在 filesDir(`DurationMemory`)。`remember_preference`(AI 在对话里静默记下用户的长期做事偏好,如"以后开会都留一小时")**仅 iOS**:落 Application Support 的 `agent-preferences.md`(`AgentPreferences`,一行一条 + 客户端去重 + 超 40 条调 `consolidatePreferences` 归纳合并),每轮 `command` 的 system prompt 里作为"用户偏好"块拼在时间上下文之后;不受 `memoryEnabled` 门控,`route()` 在进确认清单之前就把它摘走静默落盘(所以确认页/撤销都看不到它),设置 → AI 设置 → AI 偏好 可查看/编辑/重置。三份长期记忆的分工:**记忆库**存用户显式收藏的资料内容,**时长记忆**存"事项类型 → 典型时长",**偏好**存"希望 AI 以后怎么做事"——prompt 里已写明别两边都记。多 AI 服务商(DeepSeek Flash 默认/DeepSeek V4 Pro/GPT-5.6 Luna/Qwen3.5 Flash/OpenAI/通义/Kimi/智谱/自定义,key 按服务商分存;默认值统一走 `AppSettings.defaultAIProvider` 与 Android `DEFAULT_AI_PROVIDER`,前两个走同一个 DeepSeek 接口、同一把 key,只是 `model` 不同——`deepseek-flash` vs `deepseek-v4-pro`。**服务商名直接跟着模型走**:DeepSeek 的 `/models` 目前只给 `deepseek-flash` 和 `deepseek-v4-pro` 两个,别再填别的代号——老的 `deepseek-v4.1-flash` 已经下线(直接 400),`deepseek-v4-flash-vision-exp` 是被服务端静默映射成 `deepseek-flash`(能用但名字对不上),两个预设因此在 2026-09 从「DeepSeek V4 Flash Vision」「DeepSeek」改名成现在这两个。服务商名同时是 key 的存储索引,所以改名配了两张读取时的映射表(`AppSettings.renamedAIProviders` 把老的存量选择映射成新名字、`apiKeyAliases` 让新名字回退去读老名字底下存的 key,Android `SettingsRepository` 同名同义),**不改写存储、也没有一次性迁移**;老用户一律落到 flash 那档,不借改名把人换到更贵的 pro 上。Android 那份预设列表本来就比 iOS 少 RunAPI 那两项,不是这次改动引入的分叉)与 AI 个性(默认无/四预设/自定义,仅注入反问/汇总/洞察)双端一致。**"使用内置 API Key"是仅 iOS 的能力**(`BuiltInAPIKey.key(for:)`,按服务商查内置 key,目前 DeepSeek、GPT-5.6 Luna、Qwen3.5 Flash 三家有;后两个都走同一个 RunAPI 账号的 OpenAI 兼容接口 `https://runapi.host/v1/chat/completions`,只是 `model` 字段不同,都是带 reasoning 的模型,响应里会带 `reasoning_tokens`,单次请求耗时可能到十几秒,RunAPI 那边偶发过网关层 5xx/模型未开通的报错,不算 App 侧的 bug),真实 key 存在 gitignored 的 `BuiltInAPIKey.swift` 里不进仓库;Android 没有内置 key 机制,所有服务商都要求用户自己填 key,不算破坏三端对齐。

**iOS/Android 已对齐的新增能力**(本轮同步):
- **AI 批量操作撤销**:`command` 批量确认执行后可撤销,回复"撤销"(或固定短语变体)不经 AI、本地直接处理;实现在 iOS 是 `AgentHostView.performUndo`(`UndoOp` 定义仍在 `TodoListView.swift` 顶层),Android 是 `TodoViewModel.UndoOp`/`undoLastBatch`,均按"新建→删除、修改/完成→用之前快照覆盖回去、删除→用快照重新插入"的思路逐条回滚,重复事项完成一次插入的历史记录也会一并清掉。**呈现方式两端有意不同**(见下方架构差异):iOS 是一条持久的单一对话,撤销按钮直接挂在气泡上(只在最新一条可点——执行新一批必然追加一条「已完成执行」把旧那颗挤下去,所以不需要额外的归属校验);Android 的 AI 弹层一次性/无持久对话,撤销走系统 Snackbar 的"撤销"操作按钮,Snackbar 带一个批次编号(`lastUndoToken`),点撤销时核对编号——如果这条 Snackbar 还没消失、又执行完新的一批覆盖了 `lastUndo`,点陈旧 Snackbar 不会误撤销新那批,而是提示"已被覆盖"。Android 的"确认执行"按钮执行入口(`performPendingActions`)第一步就把 `pendingActions` 取走清空,防止快速重复点击并发执行两次;撤销快照(`before`)在真正落库前即时查一次 `TaskRepository.current(uuid)`,不用弹层打开时那份可能已经过时的列表快照——等待确认期间目标事项可能已被通知按钮/Siri 并发改动,用陈旧快照撤销会把并发的改动覆盖掉。
- **联网搜索 + `answer` 动作**:`command` 新增 ReAct 工具 `{"thought", "tool": "web_search", "query"}`(配置了 Tavily key 才开启,`AppSettings`/`SettingsRepository` 里的 `webSearchEnabled`/`webSearchConfigured()`)与 `{"action": "answer", "text"}`(一般性问题的直接回答,与待办操作互斥,归一化规则同 `memorize`/`ask_memory`:混着写操作时丢弃,全是 answer 时只留第一条)。**`answer` 不受任何开关门控**(2026-09 改,两端同步):"直接回话"是聊天入口的基本能力,它的 prompt 那条写在常驻的 agent 段里(iOS `AgentSkillStore.defaultAgent`、Android `command()` 的基础 system prompt),`webSearchEnabled` 只决定它答之前能不能先联网查一下。原来把 `answer` 和搜索 skill 绑在一起,没配 Tavily key 的用户一句闲聊/一张照片就会让模型交白卷(`{"actions": []}`),前端只能报错。Watch 也会收到纯回话,`WatchAgentView` 直接显示、不进"确认执行"那条路。Tavily 是接入的搜索服务(`https://api.tavily.com/search`),key 按"服务商"同一套机制分存(iOS `KeychainHelper.apiKey(for: "Tavily")`,Android `SettingsRepository.apiKey("Tavily")`)。**ReAct 循环机制两端不同**:iOS 有多轮对话历史,搜索结果作为独立 history 条目喂回模型;Android `command` 没有 history 参数,搜索结果直接拼进下一轮用户消息文本里,最多 3 轮,语义等价。**模型没按协议答话时的兜底(仅 iOS/LodoCore,Android 未跟进)**:模型返回 `{"actions": [], "reply": "…"}`(有话说但没有要执行的操作)、整段白话不带花括号、或按 prompt 约定返回 `{"error": "原因"}` 时,`command` 入口一律当成 `answer` 渲染成普通回复气泡,不再红字报错——带照片附件时最常碰上(图里有内容,用户那句话却没让它做什么)。真正给坏了的 JSON(有花括号但解析不了)仍然报错。**JSON 外壳写串时的兜底(同样仅 iOS/LodoCore)**:单条操作直接摊在顶层(`{"action": "edit_trip", …}`,漏了 `actions` 数组)按只有一条操作处理;反过来 ReAct 工具被塞进 `actions` 数组(`{"actions": [{"action": "read_trip", …}]}`)且只有这一条时按工具调用处理(工具名与能力开关的对应关系不放宽,`parseToolCall` 是顶层 `tool` 分支和这条共用的同一份)。这两种偏差在"先 `read_trip` 再 `edit_trip`"这类必须走工具的请求上会让整条路径直接报错。剩下的未知 action 报错文案带上模型编的那个名字,方便照着报错查。
- **抓取链接内容(`web_fetch`)**:与 `web_search` 同一个开关(`webSearchEnabled`)、同一个 skill 文案里追加。ReAct 工具 `{"thought", "tool": "web_fetch", "url"}`,用户消息里出现具体 http/https 链接且想了解链接内容时用,与 `web_search` 二选一(前者抓指定链接,后者搜关键词),不把链接当搜索词。**实现两端不同**:iOS 复用记忆收藏已有的 `ContentExtractor.extract(url:)`(WebKit 的 HTML→NSAttributedString 转换,`ios/Lodo/AI/ContentExtractor.swift`);Android 没有等价基础设施,`WebSearchClient.fetchUrl` 自己写了个简单的标签剥离(去 script/style/注释/标签、折叠空白,没引第三方 HTML 解析库),效果比 iOS 朴素但够 AI 理解页面大意。两端都截断到 8000 字符量级(iOS 沿用 `MemorySearch.maxSourceChars`,Android 本地常量同值)。
- **AI 思考强度**:设置项 `thinkingLevel`(off/low/medium/high,默认 medium),通过 `reasoning_effort` 字段传给支持推理的服务商/模型,只作用于 `command`(AI 助手对话入口),不影响 parse/edit/汇总等后台小请求。不支持的服务商会忽略这个多余字段。
- **AI 助手对话可中途取消**:请求进行中(含 ReAct 多轮)输入区发送按钮换成取消,点了直接 `Task.cancel()`;iOS 已做(`AgentView.sendTask`),Android 的 AI 弹层暂未跟进同款取消入口,不算破坏对齐(Android 请求普遍更快、弹层本身可以直接划掉退出)。
- **`suggestDuration` 消费点已双端对齐**:`suggestDuration`(时长记忆建议)双端函数本身同 prompt;"新建缺时长时主动 consult 这份记忆"这个消费点,iOS 是 Siri 快捷指令(`LodoIntents.swift`)+ 主聊天入口(`AgentHostView+Routing.swift` 的 `route()`),Android 是"快速添加页"(`TodoViewModel.addParse`)+ 主聊天入口(`TodoViewModel.agentRoute` 里 `AIAction.Create` 单条分支,同文件参考 `addParse` 已有写法补上)——两端各自的两个入口都已消费,不再有缺口。

**Android 记忆/收藏系统与周边能力已跟进**(核心子集,不是逐字段照抄 iOS):数据层 `MemoryEntity`/`MemoryDao`/`MemoryRepository`(`android/.../data/`),纯文字/链接收藏 + AI 整理(`DeepSeekClient.memorize`/`askMemory`,prompt 去掉了 iOS 独有的资产字段抽取指令,因为 Android 资产落库走结构化表单不经这条 AI 整理路径),`command` 协议新增 `memorize`/`ask_memory`/`suggest_memorize` action 与 `search_memory` ReAct 工具(`memoryEnabled` 门控,和 `webSearchEnabled` 同一个"能力开关传参"模式,不是设置页开关);检索退化成纯关键词(`core/MemorySearch.kt`,无端上 embedding,和 iOS"没有 embedding 时退化成关键词搜索"是同一个合法降级路径,不是残缺实现)。资产/人脉两个子功能同 iOS 思路——不是独立 `MemoryKind`,是打了保留标签(`assetTagName`="资产"/`contactTagName`="人脉")的记忆条目,额外字段直接落在 `MemoryEntity` 上;区别于 iOS 的是这两类走结构化表单直接落库(不经 AI 整理,省一次网络请求,离线也能记)。人脉关系图谱(`ContactRelationshipEntity` 无向边 + `core/ContactGraphLayout.kt` 确定性圆形布局,单测 `ContactGraphLayoutTest`,UI 是 `ContactGraphScreen.kt` 用 Compose `Canvas` 画节点连线——这是仓库里除 iOS `ContactGraphView` 外唯二的自绘 UI 例外)、通讯录导入导出(`ContactsBridge.kt`,零权限的单个路径:选择导入用系统 contact picker,导出走 `ACTION_INSERT` 确认页;批量导入导出需要 `READ_CONTACTS`/`WRITE_CONTACTS` 权限,Android 暂未跟进,iOS 独有)、zip 全量备份(`data/Backup.kt`,待办+记忆+人脉关系边一起打包,导入按 uuid 去重合并不覆盖;`version 1` 的老备份没有 `contactRelationships` 字段,导入时按空数组处理不影响其余部分)均已实现。仍为 iOS 独有:AI 对话多附件与"从记忆库选择"(Android 记忆系统目前只服务 `command` 协议与记忆 tab 本身,还没接入 agent 弹层的附件选择)。

**Android 定时任务已跟进**,触发机制是平台差异(不是需要对齐的地方):数据层 `RoutineEntity`/`RoutineRunEntity`,触发时间计算复用 `Scheduler.nextOccurrence`(把例行任务包成一次性 `TaskData` 求下一次触发,不另写一套);iOS 靠 `BGAppRefreshTask` + 预排通知兜底,Android 用 `WorkManager` 15 分钟周期检查(`RoutineCheckWorker`,15 分钟是系统允许的最小周期,同样是"尽力而为"不追求精确触发);执行只做单轮直接作答(`DeepSeekClient.runRoutine`),不像 iOS 那样带 ReAct 联网,是这一轮的明确简化;UI 在设置 → 定时任务。**定时任务不进备份 zip**,两端一致(iOS `BackupData` 没有对应结构,Android `Backup.kt` 同样没有)。

**Android 端上 AI(Gemini Nano,`ai/GeminiNanoClient.kt`)已跟进**,对应 iOS Foundation Models(`FoundationModelsClient`)在产品里的同一个位置——"不联网、不需要 API key 的本机模型",底层技术不同(Apple Foundation Models vs Google AICore SDK)是平台差异。**这项能力把 Android 的 `minSdk` 从 26 提到了 31**(AICore SDK 硬性要求,用户已确认接受掉 Android 8.0-11 支持);AICore 目前是实验阶段 SDK(`0.0.1-exp01`),端上模型只在少数 Pixel 机型真正可用,`GeminiNanoClient.isAvailable`/`generate` 失败一律返回 `false`/`null` 静默降级,不抛错误打断主流程——设置页"端上 AI"栏目点"检测"才探测一次,不在 app 启动时自动测。

**Android Siri Intents 等价物**:Google Assistant App Actions,`res/xml/shortcuts.xml` 里的 `<capability android:name="actions.intent.CREATE_TASK">` 声明(对应 iOS `LodoIntents.swift` 的 `AddTaskIntent`),语音说出的标题通过 `MainActivity.consumeRouteIntent` 的 `"create_task"` 分支送进 agent 输入框走正常解析确认流程,不跳过确认直接落库。**这条声明未经真机验证**——需要 Google 的 App Actions Test Tool 加登录 Google 账号的真机/模拟器,和没有真机的环境下无法验证 Siri Intents 是同一类限制。

**Android 桌面小组件已跟进**(`widget/ReminderWidgetProvider.kt`),经典 `AppWidgetProvider`/`RemoteViews`(不是 Glance,维持"UI 只用系统控件"的一贯做法),展示最近一条待办;系统周期刷新(30 分钟,平台允许的最小值),没有像 iOS `WidgetBridge.sync` 那样"数据变更后主动推刷新"的实时联动,是platform 触发机制差异,不是缺口。

仍为 iOS 独有(有基础设施依赖或架构差异,Android 未跟进不算破坏对齐):应用内实时语音听写(Android 用系统 `RecognizerIntent` 对话框代替;agent 入口 iOS 是侧栏底部右下角那颗「AI 助手」主操作胶囊(AI 页没有自己的导航行——它就是那颗胶囊;冷启动也默认落在这一页,见 `AppSettings.openAgentOnLaunch`,默认开)、Android 是顶栏 ✨ 按钮;Android 汇总正文在触发时现算,iOS 是前台排定时的快照)、CloudKit 自动同步(Android 换设备靠手动 zip 导入导出,不是自动云同步)。总览页(待办/记忆之外的第三个页面,`OverviewView`;聚合到期提醒+今天待办[`TaskRowView` 复用待办页同一套可向左滑完成/改期/稍等/删除、点击编辑的行]、`suggestTodayHandling`/`summarizeTodayMemories` 两句 AI 内容按天缓存)同样仅 iOS——依赖记忆数据层与已有的 DeepSeekClient 调用组合出的一个聚合视图,Android 没有对应设计,不算破坏三端对齐。

**健康分析(仅 iOS,Android 不跟进不算破坏对齐)**:Android 的等价物是 Health Connect(`androidx.health.connect`),是另一套基础设施,这轮没做。iOS 侧分三层——① 纯逻辑 `LodoCore/HealthReport.swift`(`HealthMetricKind`/`HealthSeries`/`HealthReport`,均值、"最近 7 天 vs 之前 7 天"的趋势、喂给 AI 的 `promptSummary()`),**刻意不 import HealthKit**,因为 LodoCore 还要在 macOS/watchOS 上编译,单测 `HealthReportTests` 因此不用模拟器就能跑;② 桥接 `ios/Lodo/Core/HealthKitBridge.swift`,整份实现 `#if os(iOS)` 门控、另一侧留同名空实现(macOS 没有 HealthKit),只读授权(`toShare: []`,app 从不写健康库),未授权/无数据/查询失败一律返回空报告静默降级(写法参照 `LocationHelper`);③ UI 是第五个平级页面 `HealthView` + 总览页的一句话健康板块。**隐私是这块的硬约束**:总开关 `AppSettings.healthEnabled` **默认关**,关着时健康页一个网络请求都不发;开着也只把汇总统计(日均/最近一天/环比)发给 AI,逐条原始样本不出 `HealthKitBridge`——`Info.plist` 的 `NSHealthShareUsageDescription`、设置页 footer、health skill 三处文案都是按这个口径写的,改动时别把它们说松了。`NSHealthUpdateUsageDescription` **不要加**(不写数据),entitlements 里是 `com.apple.developer.healthkit`。AI 侧:`analyzeHealth`(健康页正文 + 最多 3 条建议,解析拆成可离线单测的 `parseHealthAnalysis`)、`suggestTodayHealth`(总览那一句,和 `suggestTodayHandling` 同构),以及 ReAct 只读工具 `read_health`(`{"thought", "tool": "read_health", "days"}`,`healthEnabled` 能力开关传参,和 `webSearchEnabled`/`memoryEnabled` 同一个模式;prompt 在 `AgentSkillID.health` 这个可编辑 skill 里,定时任务那条路径不给这个工具、幻觉出来直接报错)。HealthKit 出于隐私**不告诉 app 读权限被拒**(`authorizationStatus` 只反映写权限),所以"被拒"和"确实没数据"在代码和 UI 上是同一种空态,别去写"检测是否被拒"的分支。**记忆库里的健康资料**走保留标签 `MemoryItem.healthTagName`("健康"):和"AI记录"同组——是保留标签(不能改名/删除)但**不**进 `hiddenByDefaultTagNames`,在记忆列表里正常显示;健康页的"记一笔"复用 `MemoryComposeView(presetTags:)` → `MemoryPipeline.saveText(extraTags:)`,标签在 AI 整理**之后**补(整理那一步会整体覆盖 `tags`,先打是白打),整理失败也照样打上。真实授权弹窗与真实数据读取**未经真机验证**——模拟器里没有健康数据,`--demo-health` 塞的是样本序列和假分析(用 `@State demoOverride` 放行,不写 `@AppStorage`,免得影响之后不带参数的启动),这和上面 Android App Actions 那条是同一类限制。

**旅行(仅 iOS,Android 不跟进不算破坏对齐)**:第六个平级页面。**行程项不是独立模型**——一张机票、一晚住宿、一个想去的地方,本身就是打了保留标签「旅行」(`MemoryItem.travelTagName`)的记忆条目,`travelTripUUID` 那组字段挂在 `MemoryItem` 上,和资产/人脉同一套路子。这么分的好处是订票确认单能当附件存、行程内容能被记忆搜索和"问 AI"命中(`TravelStore.create` 会把地名/航班号一起写进 `sourceText`);只有"这趟旅行本身"(名字/起止日/备注)是新模型 `TravelTrip`,与行程项靠 uuid 关联、**不建 SwiftData 关系**(理由同 `ContactRelationship`)。保留标签「旅行」和「健康」「AI记录」同组:不能改名/删除,但不进 `hiddenByDefaultTagNames`,在记忆列表里正常显示。

纯逻辑在 `LodoCore/TravelPlan.swift`(`TravelEntry` 值快照 ↔ `MemoryItem`,和 `TaskData` ↔ `TaskItem` 同一个分层),单测 `TravelPlanTests` 不用模拟器。几条容易写错的语义:**住宿按"住了几晚"铺开**(入住日到退房**前一日**,退房当天早上就走了不算那天),航班/地点只落在开始时间那天,没时间的进「未排期」,有时间但落在行程区间外的走 `outOfRange` 单独列出来(改签/记错日期时不能让它凭空消失)。价格汇总按币种分组,折算复用资产那套 `ExchangeRateStore.convert` 与 `assetDisplayCurrency`;**换不出汇率的币种不参与求和,而是原样报回 `TravelTotal.missingCurrencies` 让 UI 如实说明**——宁可少算也不能拿错汇率糊弄,别"优化"成默默当 0。

UI 是 `TravelListView`(顶部「最近旅行」总览卡——进行中优先、其次最近出发、都没有才退回最近结束的那次;下面「其他旅行」是其余进行中/即将出发的,按出发日从近到远;已结束的收在最底下的 `DisclosureGroup` 折叠栏里、默认收起;截图参数 `--demo-travel-more` 塞几次样板旅行、`--demo-travel-past-expanded` 展开折叠栏)push 进 `TravelDetailView`(顶部是旅行信息头:名字/城市·国家/日期/备注,点它进编辑;下面按天/地图/价格三个视图分段切换,没有总览)。`TravelTrip.city`/`country` 是纯展示用的手填文字,不参与地图定位。地图只画有坐标的点:坐标来自 `PlaceSearchView` 的 `MKLocalSearch` 搜地名选点(系统能力、无需 key、**不要定位权限**),用户手打地名则没有坐标、不上地图——这是有意的,不要为此去要定位权限或加地理编码兜底。

AI 两处:①「从订单导入」(`DeepSeekClient.parseTravelItems` + 可离线单测的 `parseTravelPayload`)把订票邮件/酒店确认信拆成行程项,**这条路径保留确认页**、不走"默认直接落库"那套——订单里的日期金额认错了代价不小(照着错的时间去机场),不像单条待办撤销一下就完事;② ReAct 只读工具 `read_trip`(`travelEnabled` 能力开关传参,同 `healthEnabled`/`memoryEnabled`;没有隐私开关,判据是"库里有没有旅行",一条都没有就不给这个工具、省下 prompt 那一段),prompt 在可编辑的 `AgentSkillID.travel` 里。定时任务那条路径不给这个工具。

**自动规划行程(`plan_trip`,可编辑 skill `AgentSkillID.tripPlanner`「规划行程」)**:用户在 AI 助手里说"帮我规划东京四天"时,模型返回 `{"action": "plan_trip", "trip", "start_date", "end_date", "summary", "items": [{kind, title, start, end, place, note, price, currency}]}`,解析在可离线单测的 `DeepSeekClient.parseTripPlan`(`TripPlanTests`)。几条定死的规则:① **不直接落库**——自动规划是建议不是事实,聊天里渲染成 `AgentTripPlanCard`(按天分组复用 `TravelPlan.group`,默认只展开第一天),用户点「写入行程」才调 `TravelStore.applyPlan`;写入后卡片换成"已写入 + 查看 + 撤销",撤销/重新写入**不限最新一条**(动的是快照里记着的 uuid,同新建待办那颗开关),但首次「写入行程」只在最新一条可点(被后续修改意见取代的旧规划不该再写)。卡片自己拿 modelContext 做写入并改写 `AgentMessage.tripPlanSnapshotData`(`TripPlanProposal` JSON,写入状态字段全 Optional),不经 AgentView 的回调链。② **只生成地点和住宿,不生成航班**——航班号时刻编不出来,模型给了 flight 也在解析时丢掉;住宿只写"住 xx 一带",不编酒店名房价。③ 写进已有旅行的判据是旅行名**完全一致**(忽略大小写首尾空白),不做模糊匹配,否则新建一次旅行;已有旅行的日期不跟着改。撤销删掉那次写入的行程项,旅行是那次新建的且删完变空才连旅行一起删。④ `tripPlanEnabled` 不看库里有没有旅行(从零规划本来就没有),主 app 恒为 true、Watch 不传;和 `read_trip`/`web_search` 可在同一轮 ReAct 里先用(读已有航班避开时间、查开放时间)。⑤ `plan_trip` 归 `isInformational` 那组:和写操作混在一起时丢弃。卡片消息的 `content` 存 `TravelPlan.promptSummary` 纯文字版,这样用户接着说"第二天轻松点"时,对话历史里模型看得到上一份排了什么,skill 要求它重新给一份完整规划。截图参数 `--demo-agent --demo-agent-trip-plan` / `--demo-agent-trip-plan-applied`(后者真的调 applyPlan,每次启动都会再写一遍,只用于截图)。

**调整已记下的行程(`edit_trip`,写在 `AgentSkillID.travel` 里)**:用户说"第二天改去奈良""把清水寺删了""天龙寺挪到下午"时,模型**必须先 `read_trip`**——AI 助手这条路径的 read_trip 带 `includeIDs: true`,每行末尾 `[id:uuid]`——再返回 `{"action": "edit_trip", "trip", "summary", "remove": [id], "add": [安排], "update": [{id, 只写要改的字段}]}`,解析在 `DeepSeekClient.parseTripEdit`(`TripPlanTests`,认 `[id:` 前缀、去重、既删又改以删为准)。和 `plan_trip` 相反,**这条直接执行**(用户已经说清哪天怎么改,同单条修改待办的取舍),`route()` 调 `TravelStore.applyEdit`,结果卡片 `AgentTripEditCard` 列出删/加/改,带「查看」「撤销」,撤销不限最新一条。几条定死的规则:① 定位旅行以删/改 id 所在的旅行为准,没有 id(纯新增)才按名字走 `pickTrip`(和 read_trip 同一个挑法);不属于这次旅行的 id 一律不动。② **航班不删不改**,**带附件的不删**(`MemoryPipeline.delete` 会连文件清掉,撤销恢复不了文件;改时间/名称可以),都记进 `TripEditRecord.skipped` 在卡片上如实列出,一项都没改成时回一句说明而不是空卡片。③ 撤销靠 `TripEditRecord` 里删掉/改掉那几项的整份 `BackupMemoryItem` 快照(备份结构现成的 apply(to:)),按原 uuid 写回;新增的删掉。④ 只挪开始时间没给结束时间时保持原时长;地点换了清掉坐标(不上地图也不能画错位置)。⑤ `edit_trip` 受 `travelEnabled` 门控(库里有旅行才有),归 `isInformational` 那组:混着待办写操作时丢弃——混进批量确认清单既没有卡片也撤销不了。规划卡片还没写入时改规划仍是重新给 `plan_trip`,已写入的才走 `edit_trip`(tripPlanner skill 里写明了)。截图参数 `--demo-agent --demo-agent-trip-edit`(真的写入样板规划再执行一次调整)、加 `--demo-agent-trip-edit-reverted` 再撤销,之后 `--demo-travel --demo-travel-detail --demo-travel-day` 可核对数据。

**AI 助手是单一持续对话(仅 iOS,Android 不跟进不算破坏对齐)**:一条永不结束的时间线,没有"新建对话"这回事,也没有对话标题(导航栏恒为「AI 助手」+ 一行当前 AI 模式)。`AgentThread` 已整个删掉,`AgentMessage` 全表按 `createdAt` 排序就是对话本身;"最新一条"这个判据(确认/撤销/写入行程等按钮的可点性)从"当前 thread 最新一条"变成全表最新一条,语义不变。老库里按 thread 分段的消息在启动时一次性清空(`AgentHistoryMigration`),判据是 `AgentMessage.formatVersion == 0`(新代码插入的恒为 1,轻量迁移给存量行填的是属性默认值 0)——**不用 UserDefaults 打标记**:那是单设备的,CloudKit 开着时后升级的第二台设备会按自己的标记再清一次,把第一台升级后新聊的内容一起删掉。备份里 `agentThreads`/`threadUUID`/`agentThreadCount` 三个 key 已退役,但仍写出空值占位——老版本 app 那边它们是必需 key,不写会让导入整条 decode 失败,别"顺手清理"(`BackupDataTests` 有守护用例)。消息列表与右栏 observer 都是倒序 `fetchLimit` 取数(200 / 50)再翻回正序,列表顶上给一颗「载入更早的对话」把窗口加一页(`.id(historyWindow)` 强制重建 @Query,正是原来 `.id(thread.uuid)` 那套写法)——对话永不结束,全表灌进 `@Query` 会把整条历史实例化在冷启动路径上。

**长对话自动压缩上下文(仅 iOS)**:`recentHistory` 仍只逐条回传最近 16 条,窗口之外积够 20 条就压成一段常驻摘要(`AgentConversationSummary`,Application Support 下的 JSON:摘要正文 + 水位线 + 已压条数),经 `DeepSeekClient.summaryBlock` 拼在 `historyBlock` **之前**进 system prompt。压缩函数 `summarizeConversation`/`parseConversationSummary` 是滚动摘要——上一版摘要和这批新消息一起给模型,产出合并后的一份。**不做成 @Model**:这是可重算的派生数据,丢了重压一遍就有;反过来两台设备会各自压出一条记录,而 CloudKit 那套"每个属性有默认值、无 unique 约束"的要求让它们没法靠主键去重。触发点在**回复落库之后**而不是发送之前(压缩自己也是一次网络请求,放在发送前会给这一轮平白加一次串行等待,而这一轮用上一次压出来的摘要本来就够),失败一律静默跳过、水位线不动,下一轮再试(同 `AgentPreferences.consolidateIfNeeded`)。长按气泡「修改」截断到水位线以内时要 `reset()` 摘要——被删掉的那几条已经压进去了,留着等于让模型继续记着一段用户刚亲手删掉的对话。

**AI 回复流式输出(仅 iOS)**:**协议没变**,仍然是一次 `{"actions": [...]}`,只是边收边显示——累加 SSE 的 `content` 增量,收完之后仍然交给同一个 `decodePayload` + `parseCommand`,最终结果的解析路径与非流式逐字相同。纯逻辑在 `LodoCore/AgentStream.swift`(`AgentStream.parseLine` 一行 SSE → content/reasoning 增量,行切分交给 `URLSession.bytes.lines`;`AnswerStreamScanner` 容错扫描还没收完的 JSON 抽出 `answer` 正文;`StreamThrottle` 80ms/换行节流),`AgentStreamTests` 离线单测。几条定死的规则:① 扫描器**按 `action` 的值门控**,不是只扫 `"text"` 键——`memorize`/`auto_memorize`/`suggest_memorize` 的 payload 里同样有 `text`,只扫键会把记忆正文先流进气泡再被收藏卡片顶掉;三态是"action 还没露面先缓冲 → 确认 answer 才补吐 → 别的动作永久闭嘴"。② 因此 **ReAct 三轮全开流式即可**,工具调用轮次的 JSON 里没有 `actions`,天然一个字都不吐,不需要预判哪轮是最后一轮。③ 转义序列收到一半(`\n`、`\uXXXX`、代理对只来了高位)时停在反斜杠前面,绝不吐半个。④ 回退矩阵:网关不支持 stream / 非 200 / 中途断流 / 攒出来的串解析不了,一律退回一次性请求,**退之前先 `onStream("")` 清掉已经显示的半句**(不清会留下"半句 + 完整句"两段);失败过的 endpoint 记进内存级 `unsupportedStreamEndpoints`(不落盘、不进设置页,重开 app 会再试)。⑤ **不照搬 `send(_:)` 那套"传输层错误重试一次"**——已经吐过字再重试会出现两遍文本,只在零 delta 时才允许退回。⑥ `reasoning_content` 的尾巴喂给已有的「思考中…」(`ShimmerText`):`command` 是唯一传 `thinking: true` 的入口,推理模型先吐思考后吐正文,那段空窗期比正文本身还长,只流 `answer` 的话首字时间几乎没改善。⑦ 流式预览复用 `.answer` 的卡片外观(`AgentAnswerCard`,气泡与预览共用),**不叠打字机**——`TypewriterText` 只服务 `.text`(「已取消这次操作。」这类本地兜底文案),流式本身就是逐字揭示。苹果智能那条端侧路径保持一次性。截图参数 `--demo-agent-stream`(不发网络,自己逐片喂预览)、`--demo-agent-history`(塞 220 条看窗口化)、`--demo-agent-live`(真发一次请求)。

**AI 助手右侧栏(仅 iOS,Android 不跟进不算破坏对齐)**:对话里产出的"页面"在对话旁边展示,首期只有旅行两种——`AgentInspectorTarget`(`LodoCore/AgentInspectorTarget.swift`,纯函数解析,`AgentInspectorTargetTests`):`.trip(uuid)` 显示 `TravelDetailView`(原样复用、可编辑),`.tripPlan(消息 uuid)` 是还没写入的规划,显示只读预览 + 「写入行程」(写入后自动改指向那次旅行);已撤销的调整跳过。默认展示对话里**最新**那份,新的规划/调整一到就跟过去;卡片上的「查看」改成打开右栏并定位到那张卡(不在 AI 页时回退切到旅行页)。实现在 `Views/AgentInspector.swift` 的 `AgentInspectorHost`,控制入口经 `\.agentInspector` Environment 下发(同 `SidebarChrome` 的写法)。**宽屏**用系统 `.inspector`,理想宽度取可用宽一半(左右 5:5),开着即"固定"(`AppSettings.agentInspectorPinnedKey`),可用宽 < 900pt 时经 `SidebarChrome.collapse` 收起导航侧栏;**窄屏**的 `.inspector` 会退化成 sheet,所以是和左抽屉镜像的自绘容器——整页往左拖唤出、对话左推压暗只留 44pt、点它或在上面右拖收回,和左抽屉同一套"第一帧定归属 + @GestureState 兜底"。它是抽屉容器的镜像,不是新的自绘 UI 先例。右栏拉开时经 `AgentInspectorPresentedKey` 让外壳的左抽屉手势让开,并往下覆盖一份 `hidesChrome = true` 的 `sidebarChrome` 撤掉对话页导航栏按钮(所以 AgentView 的标题拆成了 `AgentTitleView`,要在子视图里读这个值)。规划写入/撤销统一走 `TripPlanApplier`(卡片和预览共用)。截图参数 `--demo-agent-inspector`(配 `--demo-agent --demo-agent-trip-plan[-applied]` / `--demo-agent-trip-edit`)。

**航班补充信息(航站楼/值机柜台/登机口/座位/机型/状态)只来自用户导入的文本和截图,不接任何航班数据 API**(曾接过 AeroDataBox,已整个移除——别再加回联网查询)。纯逻辑是 `LodoCore/FlightDetails.swift`:整块 JSON 存在 `MemoryItem.travelFlightData`(`TravelEntry.flight`/`ParsedTravelItem.flight` 解出来),字段多且全可选,不拆成一堆列。来源是「从订单导入」:`TravelImportView` 可以选截图,截图用 `ContentExtractor.recognizeText(imageData:)` 在端上 Vision OCR 成文字、不落文件不上传图片,和粘贴的文本拼在一起交给 `parseTravelItems`;prompt 里航班项多一个可选的 `"flight"` 对象(`FlightDetails.parse` 纯函数解析,`TravelPlanTests` 离线单测)。**计划时刻进 start/end,截图上的变更/预计时刻进 `estimated_departure/arrival`**,两个都留着才算得出晚点;status 只在文本明写时填,不从时间推断。

"动态更新"= 再导入一张新截图:`TravelStore.existingFlight` 按航班号(`normalizedNumber`)+ 起飞时间相差 <20h 认出同一班,确认页上标「更新行程里已有的这班航班」,确认后 `TravelStore.merge`——补充信息走 `FlightDetails.merged(with:)`(新截图有的字段覆盖、没有的保留,不会因为只拍到登机口就清掉座位),基本信息(时间/地点/价格)**只在原来空着时才填**,不拿 OCR 结果覆盖用户确认过的内容。这条路径仍保留确认页。数据不会自己更新,所以详情页 `FlightStatusView` 始终写「更新于」,列表行的状态胶囊只在起降时间后 12 小时内显示(飞完几天还挂着「延误」只会误导)。编辑表单里航班多一段可手填的航站楼/值机柜台/登机口/座位/机型;航班号改成别的再保存时,截图带来的状态/预计时刻等字段丢掉,只留表单里那几项。行程列表里点航班一律进 `FlightStatusView`(编辑在它的工具栏里),其余类型直接进编辑表单。`read_trip` 摘要带上这些补充信息。DEBUG 参数:`--demo-travel-flight`(配 `--demo-travel --demo-travel-detail`)挂一份样板信息并打开航班详情,`--demo-travel-import` 直接打开导入页。

备份:`BackupMemoryItem` 加了那 13 个旅行字段(外加航班补充信息 `travelFlightData`)、`BackupPayload` 加了 `travelTrips`,两处都走 `decodeIfPresent` 兜底老格式。**不能只备份行程项不备份 trip**——那样恢复出来的行程项会指向一个不存在的 trip,静默退化成普通记忆条目。

**菜单(仅 iOS,Android 不跟进不算破坏对齐)**:第七个平级页面(侧栏「旅行」下面)。拍照(`CameraPicker`,桥接系统 `UIImagePickerController`——SwiftUI 没有相机控件,它本身就是系统界面,不算自绘;`Info.plist` 加了 `NSCameraUsageDescription`)/相册选截图/贴文字,三者可混给;图片在端上 OCR(`ContentExtractor.recognizeMenuText`,多语种 + 自动语言检测,和收藏那条只认中英的分开),**只发文字给 AI、不传图**。`DeepSeekClient.parseMenu`(可离线单测的 `parseMenuPayload`)一次返回店名/原文语言/币种 + 每道菜的原文名、译名(应用内语言)、分类、价格、一句话简介(菜单没写或名字看不懂时 AI 按常识补全)。**这条路径不给确认页**:认错一道菜的代价是点错菜,让人在餐厅里逐条勾五十道菜更难受;整理完直接落库,中途失败什么都不留。

数据分层和资产/人脉/旅行**反过来**:**整张菜单**是打了保留标签「菜单」(`MemoryItem.menuTagName`)的记忆条目(第一张照片当附件、清单 + OCR 原文进 `sourceText`,所以记忆搜索和"问 AI"能命中;`menuSourceLanguage`/`menuTargetLanguage`/`menuCurrency` 三个字段),**菜品是独立轻量模型 `MenuDish`**,靠 `menuUUID` 关联、不建 SwiftData 关系。理由是数量级——一张菜单三五十道菜,每道都成为记忆条目会把记忆库和向量索引刷爆,单道菜也不是值得收藏的资料。别按旅行的先例把菜品"统一"成记忆条目。`MemoryPipeline.delete` 会连带删掉该菜单的 `MenuDish`(从记忆页划掉菜单走的是这个入口);备份里 `BackupMemoryItem` 加了那 3 个字段、`BackupPayload` 加了 `menuDishes`,都走 `decodeIfPresent` 兜底。「菜单」保留标签和「旅行」同组:不能改名/删除,但正常显示。

纯逻辑在 `LodoCore/MenuPlan.swift`(`MenuDishEntry` 值快照,单测 `MenuPlanTests`):分类按菜单上**首次出现的顺序**排、不按字母重排,没分类的收在最后;`total` 一道标价的都没有时返回 **nil 而不是 0**(免得像是免费的),没标价的数量另报。勾选状态**持久化**在 `MenuDish.selected`(点菜过程会切出去再回来)。UI:`MenuListView` → `MenuDetailView`(分组列表,点行勾选;选了菜底部浮出 `glassBackground(Capsule())` 悬浮条)→ 点悬浮条拉起 `MenuOrderView`(`presentationDetents([.medium, .fraction(0.9)])`,**原文名放大在上、译名小字在下**——那张是给服务员看的,他认的是菜单上印的名字)。截图参数:`--demo-menu`、`--demo-menu-detail`、`--demo-menu-order`(不走网络,塞一张样板居酒屋菜单)。

**iOS 导航是左滑抽屉,不是标签栏**(仅 iOS,Android 是底部导航 + 顶栏 ✨,不需要跟进):总览/待办事项/记忆/健康/旅行/菜单/AI 七个页面完全平级,`AppShellView` 是唯一的导航外壳——窄屏(iPhone)侧栏是抽屉,整页往右拖唤出、内容整块推移压暗,也可以点导航栏左上角的 ☰(`sidebarToolbarButton()`,经 `\.sidebarChrome` 这个 Environment 下发,六个页面不用各加 init 参数;它带的 `go(_:)` 也是跨页跳转的统一出口,总览页健康板块点进健康页走的就是它);宽屏(iPad 常规宽度/macOS)侧栏常驻并排(判据是 `usesRegularLayout`,macOS 直接按平台定死——那边 `horizontalSizeClass` 可能是 nil,按 `== .regular` 判会掉进抽屉分支)。**抽屉推开时页面导航栏上的项要整条撤掉**,不只是 ☰:工具栏挂在 NavigationStack 上、不跟着内容平移,留着会浮在已经露出来的侧栏上面还能点(待办页「项目视图」、记忆页那一串都按 `sidebarChrome?.hidesChrome` 门控;新增页面工具栏时记得跟上)。`hidesChrome` 的判据不能只看 `showSidebar`——收起动画播完之前也得压住,所以 `closeSidebar` 用带 `completionCriteria` 的 `withAnimation`,回调里才放开 `isClosingSidebar`。**唤出手势是整页任意位置往右拖**(`.simultaneousGesture`,`sidebarDrag()` 第一帧就按"横向为主 + 方向对"定死归属,纵向滚动照常让给列表;`@GestureState` 只用来兜底——系统中断手势时不走 `onEnded`,靠它把拖到一半的位移归零)。横向可滑控件(`HorizontalChipRow`)会经 `SidebarDragExclusionKey` 这个 preference 申报自己的矩形,起手点落在里面的拖拽抽屉直接不接管,否则"往右看下一个胶囊"会顺手把抽屉拖出来;它只在当前显示的那个页面申报(`\.sectionIsActive`,六个页面是叠在 ZStack 里的,不筛的话隐藏页的胶囊行会在别的页面留下一块死区)。收回抽屉的拖拽只挂在遮罩上,**不要挂到侧栏面板上**——面板里那个 List 自己有向左滑的行操作(标签常驻),会和它抢同一个方向。为了不和它抢方向,**全 app 没有任何 `swipeActions(edge: .leading)`**——行操作一律收在向左滑那一侧,主操作(完成/转为待办/立即运行/未完成)排在最靠外当 full swipe、删除排在里面;新增行视图时别再往 leading 挂东西。记忆页工具栏左上只有 ☰(人脉态多两颗:关系图谱、批量导出),右上是收藏「+」菜单——**没有筛选按钮**,标签/资产/人脉一律从侧栏进(`consumeTagFilter`,每次覆盖不叠加),列表顶部那行「清除筛选:xx」是取消筛选的唯一入口;按来源格式(文字/链接/文件)筛这个能力随筛选浮层一起去掉了,它当时只有那个浮层一个入口。记忆页 push 进条目详情后整个手势让开(`swipeGestureEnabled`,那时左边缘归系统返回手势,所以 `memoryPath` 提到外壳持有)。抽屉容器在 `compactLayout` 的 ZStack 上挂 `.ignoresSafeArea(.container)`——**必须挂在那一层**,挂在 `sectionStack` 上时 `clipShape` 仍按安全区内的 frame 裁,推开的卡上下各短一截;只忽略 `.container`(默认的 `.all` 连键盘区一起吃,AI 输入栏会被键盘盖住)。代价是被吃掉的那截安全区要手动还:侧栏底部那排浮层按钮用 `deviceBottomInset` 加回来(和 `deviceTopInset` 同一套,量它的 GeometryReader 挂了 `.ignoresSafeArea(.keyboard)`,不然键盘一弹起量到的就是键盘高度),页面内容用 `safeAreaInset` 补 `pageBottomRefill = deviceBottomInset - keyboardInset`——`keyboardInset` 是在忽略了容器安全区的那棵子树里量出来的(那里剩下的底部安全区只有键盘),**不监听 keyboardWillShow/Hide**:那是进程级通知,浮动键盘不占底部安全区也照发,交互式收键盘的中间态也没法用一个布尔表达。ZStack 还要垫一层 `drawerBackdrop`,不然侧栏只有 300pt 宽、圆角缺口右边露的是窗口纯白;**这一层就是侧栏看上去的那层材质**(面板自己不铺底色,`sidebarPanel` 只在宽屏常驻时铺),iOS 26 起是 `GlassSurface`(Liquid Glass),旧系统退回 `panelBackground`——夜间叠两层那个对齐亮度的 hack 只留在旧系统分支里,玻璃这边侧栏和缺口本来就是同一层,不会有那道竖直明暗分界。**不要再给面板自己补一层玻璃**:玻璃采样不到玻璃,两层叠上去那块会直接失真。侧栏(`AppSidebarView`)自上而下是页面导航行(记忆行下面依次是:常驻的记忆标签平铺 → 「健康」「旅行」「菜单」三行(都是建在记忆库上的功能) → 默认折叠的"更多标签";标签行左滑切换常驻,常驻集合存在 `AppSettings.sidebarPinnedTagsKey`。三个功能行放在折叠**之前**,展开更多标签时它们不会被一长串标签挤下去)、底部浮层(左「设置」——全 app 唯一设置入口、右「AI 助手」主操作胶囊)。侧栏**没有对话列表、也没有搜索框**——AI 助手是单一持续对话,没有对话可挑也没有对话可找(清空的入口在 设置 → AI 设置)。六个页面用 ZStack 叠着只显示当前那个(切回来时筛选胶囊/滚动位置还在,和原来 TabView 一样),但没打开过的不构建(总览页一挂载就会发起 AI 请求)。AI 页的路由/暂存态(`pendingActions`、撤销快照)归 `AgentHostView` + `AgentHostView+Routing.swift`,不再寄生在 `TodoListView` 上。

## 各端架构差异(有意为之,勿"统一")

- **提醒引擎**:web 靠页面轮询;iOS 预排 8 条本地通知链(`NotificationManager`,完成/稍等时 rebuild);Android 每事项只挂**一个**精确闹钟,`ReminderReceiver` 触发时发通知→markNotified 入库→重排下一个,自我延续(requestCode = uuid.hashCode())。Android 的完成/稍等两条路径(界面按钮、通知按钮)都走 `TaskRepository`。
- **定时任务的触发(仅 iOS)**:iOS 不允许 app 后台跑定时器,所以和纠缠提醒一样两条腿走路——① `BGAppRefreshTask`(`LodoApp` 的 `.backgroundTask`,标识符 `com.lodo.app.routine`,已写进 `ios/Support/Info.plist` 的 `BGTaskSchedulerPermittedIdentifiers` + `UIBackgroundModes: fetch`)在接近计划时间时被系统唤醒,跑完**当场把结果推成通知**;② 系统不保证唤醒,所以另预排"到点提醒"通知兜底(最多 6 条,`RoutineRunner.dueBudget`,系统 64 条上限里纠缠链已占 48),到点提示打开 app,回前台时 `ContentView` 的 scenePhase 立刻补跑。**前台补跑不推通知**(人已经在看 app 了),只有后台跑完才推。同一时间槽只跑一次(`AIRoutine.lastScheduledSlot`),错过超过 6 小时(`RoutineSchedule.catchUpWindow`)就跳过等下一次。
- **iOS 分层**:`LodoCore` 是纯 Swift SPM 包(无 UI 依赖),app 层的 `TaskItem`(SwiftData)与 `TaskData` 互转后调用调度器。Android 对应:`com.lodo.app.core` 保持纯 Kotlin/JVM(无 Android import),`TaskEntity`(Room)↔`TaskData` 互转。**不要往 core 里引平台依赖**,否则单测跑不了。
- **API key 存储**:web 用 `web/.env`;iOS 用钥匙串(`KeychainHelper`);Android 用 AndroidKeyStore AES/GCM 加密后存 DataStore(`KeystoreCipher`)。
- **新系统 API 接入模式(iOS 26 / 27 同一套)**:部署目标保持 iOS 17/macOS 14,新 API 一律 `#available(iOS 26.0, macOS 26.0, *)` / `#available(iOS 27.0, macOS 27.0, *)` 运行时门控 + 旧写法回退,**并且把 `#available` 收进一个封装**,调用处只写封装名、不逐个视图重复版本判断(UI 侧见 `ios/Lodo/Views/LiquidGlass.swift`,非 UI 侧同理各收一处入口,如 `HealthMetricKind.availableKinds()`)。目前仓库里还没有任何真正的 iOS 27 API 调用——只是把门控约定摊开了,接具体新 API 时按名字填进这两处封装即可。导航容器本身不再有版本分叉——抽屉是自绘容器 + 系统 List,iOS 17 到 26 同一套代码。Liquid Glass 只用于独立主操作和系统 chrome,List 行内按钮保持 bordered——**玻璃不上内容**(列表行、正文、媒体),这是 Liquid Glass 本身的规矩,不是这个仓库的偏好。**相邻的多块玻璃要用 `glassGroup()`**(`GlassEffectContainer` 的门控封装)合进一个容器:玻璃不能采样玻璃,各自为政时挨着的两块亮度/折射对不上,而且每块各建一个 CABackdropLayer。目前三处——侧栏底部那排(齿轮 + 新建)、AI 对话的消息列(结果卡片自己是玻璃底,下面那排确认按钮也是)、AI 输入栏那一行(+ 号玻璃圆 + 输入胶囊)。单独一块玻璃不需要容器(见 `AgentView.inputBar` 的注释),隔着整屏的两块也不必硬凑。
- **编辑保存**都会重置 phase=start、nextRemindAt=remindAt;Android 的 `applyEdit` 有 PENDING 守卫。
- **交互反馈与无障碍(仅 iOS,三处封装,别绕开)**:① **按下反馈**——`.buttonStyle(.plain)` 在 SwiftUI 里按下零反馈,全 app 的图标按钮/可点卡片一律用 `PressFeedback.swift` 里的 `.pressable()`(小控件)/ `.pressableCard()`(整行整卡,缩放幅度小一档),**不要再写 `.buttonStyle(.plain)`**;曲线是临界阻尼不回弹(按下没有动量可继承,过冲显得轻浮)。② **最小触控尺寸**——HIG 是 44pt,但 AI 输入栏那几颗控件的**视觉**尺寸刻意定在 36pt(彼此必须同尺寸同圆心,见 `AgentView.composerControlSize`;输入栏是**单行**——只有两块形状:独立的 + 号玻璃圆、一路到底的玻璃输入胶囊,两块同高 36,`composerRowMinHeight` 就等于这个值。麦克风/发送/识别中**嵌在胶囊里面**靠右端,小一档(`composerInlineControlSize` = 28,加上下各 4 的胶囊内边距正好 36);**胶囊里那颗不再套玻璃**——它已经落在胶囊那层玻璃上,玻璃采样不到玻璃。+ 号和录音态的取消键用 `glassBackground(Circle())` 而**不是** `.glassButton()`:系统按钮样式自带内容内边距和 HIG 最小尺寸,外面叠 .frame 收不住,会比同排其他控件大一圈;发送/完成录音是实心强调色圆,主操作要压过玻璃这一档。内嵌那颗的 44pt 命中区会往左吃进文本框 8pt,是内嵌按钮的固有取舍,别为此把可点区缩回 28),这类地方放大的是热区不是外观:用 `.hitTarget(visualSize:)`(padding 撑开 + contentShape + 负 padding 把布局缩回去,所以间距和输入栏高度都不变)。相邻控件间距小于补出那一圈时命中区会重叠,只用于隔着 Spacer 的独立控件。③ **两个辅助功能开关**——动画一律走 `Animation.lodoAware(_:)`(**不要**直接 `withAnimation(.snappy)`);「减弱透明度」收在三个材质入口 `DesignMetrics.panelBackground`、`LiquidGlass.GlassBackground`(小块 chrome:输入栏、悬浮条)与 `LiquidGlass.GlassSurface`(整块面板:侧栏抽屉、AI 右栏),都经 `DesignMetrics.reducesTransparency(_:)` 判断,开启时换不透明面色而不是换更厚的材质。**body 里判断减弱动态效果一律用 `@Environment(\.accessibilityReduceMotion)`,不要用 `DesignMetrics.reduceMotionEnabled`**——后者是静态读,SwiftUI 不知道它变了,用户运行中改设置视图不会重建(`ShimmerText`/`TypewriterText`/`RecordingWaveform`/`EasterEggView` 都已按此写);`lodoAware` 内部那次静态读是对的,它在动作发生那一刻才求值。模拟器上 simctl 写进 `com.apple.Accessibility` 的那两个键**传不到 app**、而那两个 Environment 值是只读的没法在根上顶成 true,所以「减弱透明度」另留了 DEBUG 参数 `--demo-reduce-transparency` 做截图验证(`DesignMetrics.demoReduceTransparency`,只加强不削弱真实设置)。
- **配色与字号(仅 iOS,全在 `LodoPalette.swift` + 根上一处下发)**:① **强调色可调,默认赤陶橙**。`AccentPalette` 六档预设(赤陶/靛蓝/靛紫/松绿/玫红/石墨),存 `AppSettings.accentPaletteKey`,设置 → 强调色。每档三个槽位、每个槽位明暗双值:`accent`(彩色文字/图标/选中态)、`fill`(大块填充)、`onFill`(**填充上的文字色**)。**同一个色值不可能两种模式都合格**——赤陶 #C2410C 白底 5.18 过 AA,同值在 #1C1C1E 上只有 3.29;系统橙 #FF9500 反过来暗色 7.74、白底只有 2.20。所有预设的四项组合都核过 WCAG AA,改色值时**重算对比度**。`onFill` 在暗色下是**近黑**不是白:暗色的 fill 是亮橙,白字压上去只有 2.08(FAB 原来就是这个 bug)。**填充类控件必须显式写 `lodoAccent.onFill`**,别留 `.foregroundStyle(.white)`,也别指望 `glassProminentButton()` 自己给对——它的标签色恒为白。② **下发只有一处**:`AppShellView` 根上 `.tint(accentPalette.accent)` + `.environment(\.lodoAccent, …)`。各视图**不要再写 `.tint(.blue)` 之类硬编码色**(`FloatingAddAction` 原来就是这个问题,是全 app 唯一不跟随设置的主操作)。`Color.accentColor` 实测**跟随 `.tint()`**,可以照用;但 **sheet 是独立呈现宿主、不继承 tint**(`SettingsView` 因此自己又下发了一份),新增以 sheet 呈现的大页面时记得跟上。需要显式写出强调色的地方读 `@Environment(\.lodoAccent)`。③ **状态色和强调色分开**(`LodoColor`)。橙现在是品牌色,原来兼着的语义已重映射:逾期/航班延误/通知权限/错误文案 → `LodoColor.critical`(红),稍等 → `neutralAction`(灰),忽略 → `muted`(更深的灰,和稍等分出先后),完成 → `positive`(绿),改期 → 强调色。**别再用 `Color.red`**:系统红 #FF3B30 在分组背景上只有 3.14,当小字不合格,`critical` 取的是 #C9252D(4.91)。④ **字号刻度整体上移过一档**(subheadline→body、footnote→subheadline、caption→footnote、caption2→caption,共 206 处),同时**撤掉了 `ContentView` 根上的全局 +1 档 Dynamic Type 补偿**。那层补偿原本是在给"内容样式偏小"打补丁,副作用是把导航栏和分区标题也一起放大,导致分区标题比它统领的内容还醒目。**别把它加回来**——要调内容大小就改文字角色本身。列表主标题另加 `.weight(.medium)`:靠字重取得存在感,不占额外空间。
- **`command` payload 解析可离线单测**:iOS `DeepSeekClient.parseCommand`、Android `DeepSeekClient.parseCommandResult` 都是从网络请求里拆出来的纯函数(给 JSON payload,不发请求),测试见 `ios/LodoCore/Tests/LodoCoreTests/CommandParseTests.swift`、`android/app/src/test/java/com/lodo/app/ai/CommandParseTest.kt`。Android 纯 JVM 单测跑 `org.json` 需要 `testImplementation("org.json:json:...")`(`build.gradle.kts`)覆盖掉 `android.jar` 里全员 throw 的桩实现,否则解析逻辑测不了。
- **Xcode 工程用文件夹同步组**:`ios/Lodo/` 下新增文件自动纳入 app target,`ios/LodoWidget/` 归小组件 target,无需改 pbxproj。`ios/Support/` 放两个 target 的 Info.plist 与 entitlements(不在同步组内)。
- **iOS 小组件(LodoWidgetExtension,仅 iOS)**:SwiftData 库放 App Group `group.com.lodo.app`(`AppGroup.storeURL`,首启从默认位置迁移);app 侧 `WidgetBridge.sync` 在每次数据变更后把"即将到来"快照写进 App Group 并刷新小组件;小组件右侧"+"通过 `lodo://add` 深链弹出快速添加页。app 的 entitlements 只挂 iPhone SDK,macOS 无签名要求也能访问 Group Container。
